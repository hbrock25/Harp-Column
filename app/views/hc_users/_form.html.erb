<%= form_for(@hc_user) do |f| %>
  <% if @hc_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hc_user.errors.count, "error") %> prohibited this hc_user from being saved:</h2>

      <ul>
      <% @hc_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
